import React from 'react';

const Login = (props) => (
    <div className="main-content">
      <div className="container">
        <h1>{props.title}</h1>
      </div>
    </div>
  );
  export default Login;
  